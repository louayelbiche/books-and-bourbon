/**
 * Re-exported from @runwell/shared-tools/detection.
 * Single source of truth for signal detection functions.
 */
export {
  detectBusinessSignals,
  detectProducts,
  detectServices,
  detectPricing,
  detectBooking,
  detectCaseStudies,
  detectTeamPage,
  detectFAQ,
  detectBlog,
  detectBusinessType,
  extractPrimaryOfferings,
  extractProductEntries,
  extractFAQEntries,
  extractBlogEntries,
  extractIndustryKeywords,
  calculateConfidence,
} from '@runwell/shared-tools/detection';
